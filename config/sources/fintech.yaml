sources:
  # Klarna custom
  # Custom-scraped page — config lives in config/scrapers/
  - name: "Klarna Newsroom"
    url: "https://www.spiris.se/nyhetsrum/arkiv"
    type: scraped
    fetch_frequency_mins: 60
    groups: [news, swedish, fintech, payment, newsroom, bnpl]
    scraper: klarna
